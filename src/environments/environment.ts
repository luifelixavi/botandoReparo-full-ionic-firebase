// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCTqzghBSlEV5TL78MFXfs_aSWeoyT1s_w",
    authDomain: "reunifal-cfe6c.firebaseapp.com",
    databaseURL: "https://reunifal-cfe6c.firebaseio.com",
    projectId: "reunifal-cfe6c",
    storageBucket: "reunifal-cfe6c.appspot.com",
    messagingSenderId: "453131582961",
    appId: "1:453131582961:web:ad0591bec3709ff8531850",
    measurementId: "G-BE8DCX5B9M"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
